# blacklist
branches:
  except:
    - legacy
    - experimental

# whitelist
branches:
  only:
    - master
    - features

language: node_js
node_js:
  - "4"

addons:
  chrome: stable

script: 
  - typings install
  - npm test